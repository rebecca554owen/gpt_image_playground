# 图像模型选择器验收

final result: passed

## 对照范围与证据

- 视觉目标：用户选定的图 2，`output/model-picker-qa/reference.png`；原图位于 `/Users/qinxian/.codex/generated_images/019fd08d-a260-7b61-8b7c-87ef0889dce6/exec-77250055-62f1-4ffb-a8b2-8ff7f32d99f1.png`。
- 实现预览：`http://127.0.0.1:5181/`，在 Codex 内置浏览器实际操作并截图。
- 全图对照：`output/model-picker-qa/desktop.jpg` 与参考图在同一个查看输入中并列检查。
- 桌面 viewport / CSS size：1487 × 1058；参考与实现均为 1487 × 1058 像素，devicePixelRatio 1，无密度缩放。
- 状态：浅色、空画廊、Flare 已选、菜单展开、示例提示词未提交。实现未配置 API Key，因此按钮显示「配置 API」，不伪装成已可出图。
- 局部对照：`reference-menu.png`（538 × 530）与 `menu-detail.jpg`（520 × 517），均从上述完整截图裁切，无缩放；用于检查模型名、说明、ID、图标、选中状态和边距。
- 其他证据：`mobile.jpg`、`mobile-custom.jpg`（390 × 844），`tablet.jpg`（640 × 800），`dark.jpg`（1487 × 1058），均在 `output/model-picker-qa/`。

## 发现与修复记录

1. [P2，已修复] 初版菜单按按钮上沿定位，会遮挡编辑中的提示词。改为以整个输入栏上沿定位；最终全图中菜单位于提示词之上。
2. [P2，已修复] 初版模型触发器在工具条最左侧，六个参数同时展开，信息密度偏高。按图 2 改成左侧上传、右侧模型/尺寸/质量/数量与主按钮；格式、透明背景和审核收进「更多生成参数」，不移除任何功能。最终菜单位置为 x≈551、y=380，520 × 517。
3. [P2，已修复] 初版旧模型名称截断、说明和 ID 层级过弱。触发器宽度增至 160px，模型标题 16px、说明 14px，辅助文字对比提高。长自定义 ID 仅在紧凑触发器省略，菜单里完整换行显示。
4. [P2，已修复并复验] 跨越 640px 断点时，隐藏的桌面选择器仍可能保留 portal 菜单，与移动端按钮状态不一致。改为按当前断点只挂载一个输入栏选择器。桌面→手机、手机→桌面两次实测均清理旧菜单（菜单数 0），重新打开只有一个菜单；最终桌面/手机截图已更新。
5. 全图与局部图重新与参考图同屏比较；手机菜单限制高度并可滚动，自定义输入与关闭操作可访问；640px 视口 documentWidth=640，无横向溢出。

## 五项视觉检查

- 字体：沿用项目 HarmonyOS/PingFang 中文无衬线字体与等宽模型 ID；标题、说明、次要文字层级明确。与生成稿的字形/字重有小幅差别，不影响辨识。
- 间距：菜单宽度、行组、分隔线与选中背景保持图 2 的结构；提示词不被遮住，长文在手机上换行并滚动。
- 颜色：蓝色主交互、浅蓝选中状态；Flare 绿色速度标签、Sunburst 蓝色质量标签。深色模式使用项目现有颜色，不改变系统主题。
- 图像/图标：目标没有需要生成的照片或插画。新图标使用项目已安装的 Phosphor 图标库；不使用装饰性占位图或手绘 SVG 替代。
- 内容：两个准确模型 ID、速度/质量定位、旧模型、自定义入口、官方指南链接与渠道提示齐全。介绍依据 OpenAI 官方 image-prompting 文档；不承诺固定提速、低价或渠道可用性。

## 交互与兼容检查

- Flare → 刷新：选择仍保留。
- 方向键切换焦点、Enter 选择 Sunburst；Esc 关闭菜单并恢复触发器焦点。
- 设置页中 Esc 只关闭模型菜单，不关闭底层设置。
- 自定义 ID 可以保存；空输入不可提交；手机滚动后可访问输入框。
- Responses 的图像模型与 `gpt-5.5` 对话模型独立；「API 默认」仅清空图像工具模型。
- 输入栏与设置页共用选择器并同步；旧模型的尺寸匹配逻辑保持不变。
- 更多参数可以展开与收起，格式、透明背景、审核仍可访问，不重复显示主参数。
- 浏览器错误/警告日志检查结果：空。
- 359 项前端测试、36 项服务端测试通过；构建通过，保留项目已有 chunk size 提示。

## 有意保留与边界

- 保留生产项目原有画廊/视频/Agent 导航、搜索与工单入口，不照搬生成稿中额外的导航和账户按钮。
- 不切换用户已有默认模型，不新增后台渠道、不改变计费或透明背景实现。
- 本验收记录覆盖本地界面实现与验证，没有进行付费生图。用户已确认上线，发布状态以 CI 和线上核验为准。

## 后续微调

- [P3] 可进一步微调字体光学字重，使其更接近生成稿；不影响本次选择器使用。

## 实施检查清单

- [x] 两个 2.5 模型、介绍、官方来源与自定义入口
- [x] Images / Responses 配置隔离与刷新保存
- [x] 桌面、手机、窄视口、深色与键盘检查
- [x] 最终截图、局部对照、测试和构建
- [x] 用户确认并授权提交与上线
